{
  "name": "pi-trust-policy",
  "version": "0.1.4",
  "description": "Bash command allowlist system for pi — grouped trust policies with glob matching, recursive parsing, and an interactive TUI manager",
  "keywords": [
    "pi-package",
    "trust-policy",
    "permissions",
    "bash",
    "allowlist",
    "security"
  ],
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/biratkk/pi-trust-policy"
  },
  "author": "biratkk",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "dependencies": {
    "minimatch": "^10.2.5",
    "unbash": "^3.0.0",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.75.4",
    "@types/node": "^22.0.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
